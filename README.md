# Car-Parking-Detector
Camera-Based Parking Occupancy Detector uses a fixed camera and a custom-built ANN (NumPy only) to classify parking slots as Vacant or Occupied in real time. Images are self-collected, features are extracted manually, and the model is trained from scratch no pretrained models or online datasets used.
